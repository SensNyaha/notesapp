# Tasks — этап 13, версия 0.13.0

Версия 0.13.0 добавляет зашифрованный жизненный цикл заметки: основной список, архив и корзина разделены, каждый служебный экран имеет локальный поиск и сортировку. Архивирование приостанавливает расписание. При восстановлении архивной заметки приложение каждый раз спрашивает, возобновить ли сохранённое напоминание; восстановление из корзины оставляет его выключенным.

История расшифровывается на открытом устройстве и показывает все неизменяемые ревизии с временем и названием устройства. Восстановление копирует содержимое, теги, чек-лист и вложения в новую ревизию, сохраняет текущую версию в истории и не включает старое напоминание. На телефоне действия заметки остаются в нижней панели, а версии просматриваются последовательно; на широком экране текущая и выбранная версии показываются рядом.

Корзина хранит заметку 30 дней. Окончательное удаление стирает все её зашифрованные ревизии и расписание с сервера; постоянная техническая метка из ID хранилища/объекта блокирует повторную загрузку со старого офлайн-устройства. Несинхронизированная поздняя версия на таком устройстве переносится в локальный зашифрованный стеш и может быть сохранена как новая заметка. Серверу доступны только ID, состояние корзины и сроки очистки; название, текст, теги и история остаются зашифрованными.

Миграция SQLite 9 → 10 добавляет таблицу `note_lifecycle` без изменения существующих записей. Перед локальной миграцией создана и проверена копия `/data/before-stage13-schema10-20260914.sqlite` и `backups/before-stage13-schema10-20260914.sqlite`; SHA-256: `6CAF1A76AB5E127C3CC581BCEA2F511CD4882D95B3139328EF96954193A8468C`. Для отката к 0.12.0 нужна эта копия схемы 9.

Ниже сохранено описание предыдущих этапов.

Версия 0.12.0 добавляет календарные повторяющиеся напоминания: ежедневно, по выбранным дням недели, каждые N дней и ежемесячно с выбором поведения для короткого месяца. Расписание заканчивается никогда, по включительной дате или после заданного количества срабатываний. На одну заметку действует одно расписание; его отдельные срабатывания имеют собственную историю, могут выполняться, пропускаться и откладываться независимо от следующего календарного срока.

Экран «Сегодня» группирует срабатывания на просроченные, сегодняшние, завтрашние, более поздние и пропущенные, показывает счётчики и фильтр по тегам текущего открытого хранилища. Из карточки доступны заметка, редактор расписания, выполнение, пауза, удаление всей истории и snooze на 15 минут, час, три часа, завтра, выходные или выбранный срок. Кнопка добавления предлагает создать заметку с напоминанием либо выбрать существующую. На iPhone действия и snooze складываются в компактные панели; на широком экране остаются внутри карточки.

Событие «Весь день» использует настраиваемое время аккаунта, по умолчанию 09:00. Важность выделяет карточку и задаёт Web Push urgency `high`, но не меняет календарный порядок. Режим push по умолчанию (`нейтральный` или `заголовок`) хранится зашифрованным в каталоге конкретного хранилища; в каждом напоминании его можно переопределить, а собственный текст остаётся выбором только конкретного напоминания. Заголовок в режиме title обновляется автоматически без создания нового расписания.

При восстановлении после простоя одноразовое напоминание отправляется один раз независимо от возраста. Для повторяющегося расписания отправляется только последнее наступившее срабатывание, а более старые получают статус «пропущено», поэтому накопленного шквала push нет. Snooze и следующий обычный срок сосуществуют. Переход по конкретному push подтверждает именно его occurrence; удаление расписания каскадно удаляет его историю.

Миграция SQLite 8 → 9 добавляет `reminder_occurrences`, календарное правило расписания, связь очереди доставки со срабатыванием и время событий «Весь день». Перед локальной миграцией сохранены и проверены `/data/before-stage12-schema9-20260914.sqlite` и `backups/before-stage12-schema9-20260914.sqlite`; SHA-256: `046AE19EF81C1256982CBB3C26C44F37015A06952697ABEFEB48357B2EECB8A5`. Для отката к 0.11.0 нужна эта копия схемы 8; Git не понижает схему SQLite.

Проверка 2026-09-14: локальная и контейнерная production-сборки, typecheck и 62/62 теста прошли; production audit — 0 уязвимостей. Пересобран тот же `tasks-local-app-1`, образ `tasks:0.12.0`, healthy на `http://localhost:3100`. Schema 9 проходит `integrity_check`; сохранены прежние 2 аккаунта, 1 хранилище и 8 зашифрованных ревизий. Подключённый браузер в среде агента недоступен, поэтому адаптивный экран «Сегодня» и реальные повторяющиеся push остаются пользовательской приёмкой.

Версия 0.11.0 добавляет организацию заметок. В каждом хранилище есть собственный зашифрованный каталог тегов с базовым набором; теги можно создавать, переименовывать, перекрашивать и удалять. Несколько выбранных тегов работают как «И». Закреплённые заметки идут первыми, доступны быстрые фильтры и сортировка. Локальный полнотекстовый поиск ранжирует результаты по релевантности, ищет отдельные слова запроса в заголовке, тексте, чек-листе и тегах всех открытых хранилищ, учитывает части слов, окончания и умеренные опечатки. Совпадения только одного из слов тоже показываются. В поиске можно полностью заменить релевантность прямой или обратной сортировкой по дате; закрепление на поисковый порядок не влияет. Содержимое закрытых хранилищ в поиск не попадает.

Чек-лист хранится внутри зашифрованной заметки отдельно от форматированного текста: пункты можно добавлять, удалять, переставлять и отмечать выполненными, в том числе прямо в режиме просмотра. Меню заметки позволяет закрепить её, перейти к изменению тегов и создать копию. Копия сохраняет текст, форматирование, чек-лист, теги и вложения; она не закреплена, а её напоминание выключено и получает новый ID. Серверная схема остаётся 8, миграция и резервная копия БД для этого обновления не нужны.

Версия 0.10.1 показывает «Название (закрыто)» на других устройствах. Названия теперь являются открытыми метаданными аккаунта, доступными серверу и его резервным копиям; фразы и содержимое заметок остаются зашифрованными. Для старых хранилищ обновите приложение и выполните синхронизацию на устройстве, где они открыты, затем синхронизируйте второе устройство. Если ключ нигде не сохранён, для первичной передачи названия требуется один раз открыть старое хранилище.

В диагностике администратора добавлен диск постоянных данных: общий объём, занятое и доступное для записи место, остаток в процентах, предупреждения при ≤15% и ≤5%. Обновление — вручную и каждые 60 секунд на открытом экране. Измеряется файловая система DATA_DIR внутри контейнера; это не RAM и не сумма всех дисков хоста. API `/api/auth/diagnostics` доступен только администратору и не кешируется.

Финальный продукт не будет иметь фиксированного лимита размера файла или общей суммы вложений заметки. Планируется потоковая загрузка зашифрованными частями с возобновлением: ограничение размера одной криптографической записи не ограничивает размер файла. Фактическая загрузка зависит от свободного места; при его нехватке нужна корректная остановка и последующее продолжение. Текущие 512 КБ — временная граница встроенных MVP-вложений до этапа 18.

Для 0.10.1 требуется миграция SQLite 7 → 8 (таблица `vault_labels`, зашифрованные заголовки и записи не переписываются). Перед локальной миграцией проверен backup `/data/before-vault-labels-schema8-20260914.sqlite`, копия — `backups/before-vault-labels-schema8-20260914.sqlite`. Перед серверным обновлением также создайте backup. Возврат к версии со схемой 7 требует восстановления backup; старый сервер не открывает схему 8.

Проверка 0.10.1: сборка и 55/55 тестов прошли локально и в Docker, production audit — 0 уязвимостей. Штатный контейнер healthy на 3100, schema 8, integrity_check — ok, 2 аккаунта/1 хранилище/8 ревизий сохранены. Проверены миграция, изоляция имён по владельцу, получение старого названия новым устройством и доступ к диагностике только для администратора. Визуальная проверка пользователем: обновить оба устройства, синхронизировать открытые хранилища на первом, затем второе; проверить «Название (закрыто)» и карточку «Диск сервера» в диагностике администратора.

Версия 0.10.0 добавляет форматированный основной текст заметки: заголовки, шрифты и размеры, полужирное/курсив/подчёркивание/зачёркивание, цитаты, код, маркированные и нумерованные списки, таблицы, очистку оформления и клавиатурные сокращения. Изображения и файлы прикрепляются к зашифрованной заметке, показываются в просмотре и доступны для скачивания; временный предел MVP — 512 КБ на файл и на сумму вложений заметки. HTML очищается перед хранением и показом. Старые текстовые заметки открываются без преобразования данных.

В настройках напоминания появился режим «Взять заголовок как текст сообщения». Он передаёт первые 200 символов заголовка в push и автоматически создаёт обновлённую настройку напоминания при последующем изменении заголовка. Как и для собственного текста, пользователь явно подтверждает передачу незашифрованного текста серверу и push-службе.

Версия 0.9.1 разделяет просмотр и редактирование заметки. Открытие строки или переход из push показывает заметку без полей ввода. На телефоне редактирование открывается кнопкой из просмотра; на широком экране эта кнопка дополнительно доступна рядом со строкой заметки. После автосохранения редактор возвращает к обновлённому просмотру.

Проверка 2026-09-14: локальная и контейнерная production-сборки и 53/53 теста прошли, production audit — 0 уязвимостей. Штатный `tasks-local-app-1` пересоздан из образа `tasks:0.10.0`, healthy на `http://localhost:3100`; схема осталась 7, `integrity_check` — `ok`, прежние 2 аккаунта, 1 хранилище и 8 зашифрованных ревизий сохранены. Визуальный браузер в среде агента недоступен, поэтому адаптивное поведение, редактор и переход из реального push проверяет пользователь.

Этап 09 добавляет разовые напоминания заметок. Дата, время, состояние и выбранный режим текста входят в зашифрованную версию заметки. Сервер отдельно хранит только необходимые для доставки метаданные: аккаунт, ID хранилища/заметки/версии настройки, местные дату и время, вычисленный момент срабатывания, состояние доставки и явно разрешённый собственный текст. Ключ и основной текст заметки серверу не передаются. Название хранилища доступно серверу с версии 0.10.1; заголовок заметки раскрывается только при выбранном режиме текста push.

Напоминание отправляется на все устройства с активной push-подпиской этого аккаунта, включая закрытые на устройстве хранилища. По умолчанию непросмотренный push повторяется каждый час; в разделе «Уведомления» доступны «не повторять», 1, 3, 6 и 24 часа. Переход по push либо открытие заметки на любом устройстве прекращает повторы, но не ставит «Выполнено». Такое подтверждение сохраняется локально при отсутствии сети и отправляется при синхронизации.

При каждом открытии приложения онлайн часовой пояс аккаунта меняется на пояс устройства. Ещё не сработавшие напоминания сохраняют местные дату и время и получают новый абсолютный срок. Конфликт версий приостанавливает серверное напоминание до выбора версии; ручного объединения нет. Копии, перенос в другое хранилище и извлечение из стеша получают новый ID и выключенное напоминание. Удаление источника удаляет его серверное расписание.

В интерфейсе добавлены действие «Напомнить» и карточка напоминания в редакторе, минимальная форма даты/времени с быстрыми вариантами, нейтральный или собственный текст до 200 символов с явным согласием, действие «Выполнено», список «Сегодня» и глубокий переход из push через вход и открытие хранилища. Визуальный редизайн из `docs/mockups/new_designs` намеренно оставлен на финальный этап; сейчас сохранена существующая MVP-стилистика.

Проверка 2026-09-13: штатный образ `tasks:0.9.0` собран на Node 24, production-сборка и 52/52 теста прошли, аудит production-зависимостей — 0 уязвимостей. Существующий `tasks-local-app-1` пересоздан на `http://localhost:3100` и находится в состоянии healthy. После миграции схема 7 проходит `integrity_check`; сохранены прежние 2 аккаунта, 1 хранилище и 8 зашифрованных ревизий. Installation ID не изменился, boot count равен 24. Резервная копия схемы 6 находится в `/data/before-stage09-local-20260913.sqlite` и `backups/before-stage09-local-20260913.sqlite`, SHA-256: `b063acb962a01a4c3c8fba146416eb0143cd4f4dcffc8388ab6a7942ebcdf1ed`. Реальный сценарий напоминания через HTTPS/iPhone остаётся пользовательской приёмкой; commit/push и серверный деплой агент не выполнял.

## Обновление 0.9.0 и проверка напоминаний

Перед первым запуском 0.9.0 сохраните резервную копию БД. Миграция 6 → 7 добавляет настройки часового пояса/повторов, серверные расписания и устойчивую очередь доставок:

```powershell
docker compose exec -T app node server/cli/backup.mjs /data/tasks.sqlite /data/before-stage09.sqlite
docker compose -f compose.yaml -f compose.ca.yaml build app
docker compose -f compose.yaml -f compose.ca.yaml up -d --no-build --force-recreate app
docker compose ps
curl.exe -fsS http://localhost:3100/api/health
```

Сохраните снимок также вне контейнера. Git не откатывает схему SQLite: для возврата к 0.8.0 нужен снимок схемы 6, а текущую БД схемы 7 нужно сохранить отдельно. Не удаляйте Compose volume. Серверный деплой и commit/push выполняет пользователь.

Проверка на HTTPS/iPhone:

1. Обновить оболочку PWA, открыть заметку, нажать «Напомнить» и задать время на несколько минут вперёд. Для первой проверки оставить нейтральный текст.
2. Закрыть хранилище только на iPhone и дождаться push. Он должен прийти без заголовка и текста заметки. Нажатие должно открыть приложение, попросить фразу и затем открыть нужную заметку; само напоминание не становится выполненным.
3. Создать второе напоминание со своим текстом и подтвердить предупреждение. Только этот текст может появиться в push. Проверить получение на всех включённых устройствах аккаунта.
4. Не открывать третье напоминание: по умолчанию push повторится примерно через час. Открытие заметки вручную на другом устройстве должно остановить следующие повторы. Интервал меняется в «Уведомлениях» для всего аккаунта.
5. Создать конфликт заметки на двух устройствах: отправка должна приостановиться. После выбора версии её напоминание синхронизируется; варианты «Сохранить обе» получают выключенные напоминания в дополнительных копиях.
6. Изменить часовой пояс устройства и снова открыть приложение онлайн. Будущий срок должен сохранить введённые местные дату и время. Время из несуществующего часа при переходе на летнее время отклоняется.

Отключённый push не отменяет расписание: после повторного включения последующее напоминание или повтор снова сможет прийти. Уже переданный push отозвать невозможно. Одна попытка доставки актуальна час; при 429/5xx выполняется до пяти ограниченных попыток, неопределённый сетевой результат автоматически не повторяется до следующего интервала напоминания.

2026-09-13 пользователь подтвердил: тестовый push успешно приходит на iPhone (iOS 26.6.1, установленный PWA, HTTPS). Замер KDF на телефоне остаётся отдельной проверкой.

В версии 0.8.0 был добавлен раздел **«Уведомления»**: инструкция установки PWA, отдельные состояния разрешения/подписки, включение и отключение текущего устройства и нейтральный тест через 10 секунд. Версия 0.9.0 использует уже проверенный транспорт для настоящих напоминаний.

Проверка 2026-09-13: штатный образ `tasks:0.8.0` собран на Node 24, typecheck/build и 47/47 тестов прошли, аудит при сборке — 0 уязвимостей. Push-служба в тестах подменена; показ на реальном iPhone проверяется пользователем после HTTPS-деплоя. По последующему разрешению пользователя прежний локальный `tasks-local-app-1` обновлён на `http://localhost:3100`, статус healthy. Backup перед миграцией: `/data/before-stage08-local-20260913.sqlite` и `backups/before-stage08-local-20260913.sqlite`. Сравнением подтверждена сохранность аккаунтов, хранилищ, записей и допусков.

## Обновление 0.8.0 и проверка push

Деплой на сервер, HTTPS и CI выполняет пользователь. Локальная отладка по его разрешению использует прежний контейнер на 3100; тег локального образа в Compose обновлён до 0.8.0. Исходники используют существующий `APP_ORIGIN`; адрес установки пользователя — `https://notesapp.sensnyaha.ru`. В локальном HTTP-режиме реальная отправка отключена. В HTTPS-режиме ключи VAPID создаются автоматически при первом включении/проверке и сохраняются в SQLite; дополнительные секреты ENV не требуются.

**Перед первым запуском 0.8.0 сохраните резервную копию БД:** миграция 5 → 6 добавляет таблицы уведомлений и триггер отзыва подписок. Например, через уже работающий сервис (с уникальным именем снимка):

```powershell
docker compose exec -T app node server/cli/backup.mjs /data/tasks.sqlite /data/before-stage08.sqlite
```

Снимок также нужно сохранить вне контейнера. Git не откатывает БД: прежняя 0.7.1 не запускается со схемой 6. Для отката нужен снимок схемы 5 и отдельное сохранение новых данных. CI и серверные настройки агентом не менялись; перед серверным обновлением нужна отдельная резервная копия серверной БД.

Проверка после вашего обновления:

1. На iPhone открыть PWA с иконки «Домой», обновить оболочку, войти постоянным аккаунтом и открыть «Уведомления».
2. Нажать «Включить уведомления», разрешить системный запрос. Должны появиться «Разрешено» и «Активна». При отказе изменить разрешение в настройках iOS и нажать «Проверить снова».
3. Нажать «Отправить тест через 10 секунд», свернуть PWA или заблокировать телефон. Ожидается нейтральное уведомление Tasks. Нажатие возвращает в приложение. Принятие сообщения push-службой не подтверждает его показ на телефоне.
4. Отключить уведомления: новый тест недоступен, ожидающее задание отменено. Включить снова, запланировать тест и выйти либо выбрать «Войти снова / другой аккаунт»: прежняя подписка и ожидающий тест отключаются; новый аккаунт включает push отдельно. Переключение аккаунта завершает серверный сеанс, сохраняя локальные профили/хранилища. Уже отправленный push отозвать невозможно.
5. Проверить, что после перезапуска сервера остаются подписка и ключ VAPID. Тест, который ещё актуален, отправляется; просроченный отменяется. После нажатия «Отправить тест» можно повторять проверку не чаще раза в 30 секунд. Срок актуальности — одна минута, максимум три попытки при 429/5xx. При неопределённом сетевом результате автоматический повтор не выполняется.
6. В «Диагностике» выполнить «Проверить шифрование» на iPhone и записать модель, iOS и показанные длительности для окончательного выбора стоимости KDF. Установка PWA и получение тестового push уже подтверждены пользователем; замер шифрования ещё не предоставлен.

Поддержаны стандартные службы Safari (`web.push.apple.com`), Chrome/Edge (`fcm.googleapis.com`) и Firefox (`updates.push.services.mozilla.com`). Произвольные адреса и перенаправления для отправки не используются. Сервер хранит endpoint и ключи транспортного шифрования подписки, случайный ID устройства, привязку к аккаунту/сеансу и состояние теста. Название устройства, хранилища и заметки в push не передаются. Тестовые записи удаляются через семь дней, подписки — при отключении/отзыве сеанса или ответе службы 404/410.

## История этапа 07

При запуске автоматически выбирается последнее использованное хранилище для этого аккаунта на устройстве. Закрытое хранилище остаётся закрытым до ввода фразы; удалённое не выбирается. Этап 07 принят пользователем, эта доработка добавлена по его запросу.

Добавлены закрытие выбранного хранилища на всех устройствах, независимый повторный допуск по фразе, сравнение конфликтов, выбор версии / сохранение обеих и название устройства. Ручное объединение не планируется. Сетевые задержки не удерживают локальное сохранение; ошибка одного хранилища не останавливает остальные очереди. Схема SQLite — 5, порт — http://localhost:3100.

Этапы 03–06 приняты. Этап 07 ожидает визуальной приёмки. HTTPS и проверка Safari/iPhone остаются следующему этапу.

Поставка 2026-09-12: 42/42 теста и typecheck/build прошли на Node 24, контейнер healthy, API 0.7.0/ok. Сравнение с backup подтвердило сохранность двух аккаунтов, одного хранилища и восьми зашифрованных версий. Подключённый браузер для автоматической визуальной проверки недоступен.

## Обновление и проверка этапа 07

Снимок перед миграцией 4 → 5 сохранён в `/data/before-stage07-20260912.sqlite` и `backups/before-stage07-20260912.sqlite`. Пересобирается существующий Compose app. Обычная сборка выполняет тесты на Node 24. При повторном обновлении сначала сделайте новый снимок с уникальным именем, затем выполните команды по одной:

```powershell
docker compose -f compose.yaml -f compose.ca.yaml build app
docker compose -f compose.yaml -f compose.ca.yaml up -d --no-build --force-recreate app
docker compose ps
curl.exe -fsS http://localhost:3100/api/health
```

Git не откатывает схему БД: возврат к 0.6.0 требует восстановления копии схемы 4 с сохранением новых данных отдельно. Не удалять volume.

1. Назовите устройства, измените одну заметку независимо в двух браузерных профилях и синхронизируйте. В сравнении проверьте текст и источник. «Сохранить обе» оставляет отдельные заметки, выбор — одну актуальную. Исходные версии остаются на отдельном экране истории.
2. Закройте выбранное хранилище на всех устройствах, подтвердив пароль аккаунта. Текущее закрывается сразу, другое — при синхронизации. Остальные хранилища сохраняют доступ. Повторное открытие на одном устройстве не открывает другое.
3. Оставьте на втором устройстве офлайн-изменение перед общей блокировкой. После подключения ключ удаляется, очередь сохраняется; введите фразу заново и синхронизируйте. Без сети загруженные данные по-прежнему открываются по фразе.
4. При доступном обновлении откройте две вкладки: обновление должно предложить закрыть остальные. Дождитесь сохранения их черновиков, закройте их и повторите обновление.

Блокировка не отзывает уже скопированные ключи и не может мгновенно закрыть офлайн-устройство. Старую оболочку нужно обновить; серверный запрет синхронизации действует сразу. Подробности протокола и раскрываемых метаданных — в [DECISIONS.md](docs/DECISIONS.md), решение 012.

## История этапа 06

Реализованы аккаунты и администрирование, несколько зашифрованных хранилищ, текстовые заметки, сохранённый локальный доступ, офлайн-очередь, сохранение обеих конфликтующих версий, перенос при забытой фразе и зашифрованный стеш. Preact/TypeScript/Vite, Fastify/Node 24, SQLite WAL, схема 4. Порт — http://localhost:3100.

Этапы 03–05 приняты пользователем. Этап 06 готовится к приёмке. HTTPS пока нет; проверка на iPhone и замер KDF перенесены до его появления. Push, поиск, теги, чек-листы и подробный интерфейс разрешения конфликтов остаются последующим этапам.

## Обновление этапа 06

Перед миграцией 3 → 4 обязательна резервная копия. Пересобирается существующий Compose app, отдельные тестовые контейнеры не создаются. Обычная сборка запускает все тесты на Node 24. Команды выполнять по одной, прекращая при ошибке:

```powershell
docker compose exec -T app node server/cli/backup.mjs /data/tasks.sqlite /data/before-stage06.sqlite
docker compose -f compose.yaml -f compose.ca.yaml build app
docker compose -f compose.yaml -f compose.ca.yaml up -d --no-build --force-recreate app
docker compose ps
curl.exe -fsS http://localhost:3100/api/health
```

Если файл снимка уже существует, используйте новое имя. Подготовлен снимок схемы 3 `/data/before-stage06-20260911.sqlite` и копия `backups/before-stage06-20260911.sqlite`. Перед финальной поставкой 2026-09-12 сохранены текущие данные промежуточной 0.6.0 (схема 4) в `/data/before-stage06-final-20260912.sqlite` и `backups/before-stage06-final-20260912.sqlite`. При откате к коммиту этапа 05 нужна также согласованная процедура восстановления схемы 3: Git не откатывает БД. Не удалять volume и не подменять рабочую БД без сохранения новых данных этапа 06.

Финальная обычная Docker-сборка прошла 37/37 тестов на Node 24, typecheck/build; аудит зависимостей сообщил 0 уязвимостей. После поставки API отвечает 0.6.0/ok, installationId сохранён. Автоматическое сравнение с последним backup подтвердило неизменность аккаунтов и зашифрованных записей. Визуальная проверка пользователем ещё требуется.

## Проверка хранилищ и заметок

1. Войти постоянным аккаунтом; создать два хранилища с разными фразами от 6 любых символов. Создать текстовую заметку, дождаться «Сохранено на устройстве», нажать «Готово» и синхронизировать.
2. Перезагрузить страницу: доступ на этом устройстве сохраняется. Нажать «Закрыть хранилище»: его ключ удаляется только здесь. Неверная фраза не создаёт пустое хранилище; правильная открывает прежние заметки. Второе хранилище остаётся открытым.
3. После загрузки данных отключить сеть, перезапустить приложение, выбрать локальный аккаунт и открыть хранилище по фразе. Изменить заметку, дождаться локального сохранения. Вернуть сеть и проверить синхронизацию. Для нового устройства сначала нужен вход и загрузка с сервера.
4. В другом браузерном профиле войти тем же аккаунтом и открыть хранилище своей фразой. Изменить одну заметку независимо на двух устройствах, затем синхронизировать: обе версии должны оставаться в списке с соответствующей подписью.
5. На первом устройстве выбрать «Забыл фразу · перенести», задать новое название/фразу и подтвердить удаление источника. После завершения старое хранилище удаляется на сервере. На втором устройстве с неотправленными изменениями появится стеш: перенести в открытое хранилище, оставить на потом или удалить с подтверждением. Закрытый источник потребует прежнюю фразу для чтения его локальных изменений.
6. Проверить выход с неперенесённым стешем: отмена сохраняет данные; подтверждение удаляет локальные данные и ключи. «Войти снова / другой аккаунт» позволяет сменить серверный вход с сохранением локальных профилей; это отдельное действие от выхода с очисткой.

Фраза не сохраняется приложением и не отправляется на сервер. Сохраняются неэкспортируемые CryptoKey в IndexedDB; это не аппаратный сейф. Очистка данных сайта удалит локальные ключи и стеш. Без фразы и устройства с сохранённым доступом восстановить содержимое нельзя; recovery-кодов и административной расшифровки нет.

Редактор показывает завершённое локальное сохранение только после транзакции IndexedDB. Последний ввод до фиксации может потеряться при принудительном завершении браузера. При ошибке сохранения не закрывайте страницу и используйте повтор. Временные границы MVP: 1 МиБ открытого JSON записи, 100 активных хранилищ, 10000 неизменяемых версий или 64 МиБ шифротекста на хранилище. При достижении лимита локальная очередь не удаляется; актуальные заметки можно перенести в новое хранилище.

Обычное закрытие не меняет другие устройства. «Закрыть на всех устройствах» записано на следующий этап по решению пользователя. Текущие автоматические тесты используют fake-indexeddb и Fastify inject; проверка Safari/реального браузерного хранилища отдельно необходима. Подробности: `docs/ARCHITECTURE.md`, `docs/CRYPTO_FORMAT.md`, `docs/VAULT_TRANSFER.md`.

## История: обновление и проверка этапа 05

Схема SQLite остаётся 3, аккаунты и сессии сохраняются. Пересобирается существующий app; отдельные тестовые контейнеры не создаются. Обычная сборка включает typecheck/build и все тесты. Из корня проекта, по одной команде с остановкой при ошибке:

```powershell
docker compose exec -T app node server/cli/backup.mjs /data/tasks.sqlite /data/before-stage05.sqlite
docker compose -f compose.yaml -f compose.ca.yaml build app
docker compose -f compose.yaml -f compose.ca.yaml up -d --no-build --force-recreate app
docker compose ps
curl.exe -fsS http://localhost:3100/api/health
```

Ожидаются 0.5.0, healthy и прежний installationId. При уже существующем снимке выберите новое имя. Агент сохранил свежий снимок /data/before-stage05-final-20260911.sqlite перед финальным обновлением; более ранний /data/before-stage05-20260911.sqlite также сохранён. Копию на хост можно выгрузить через docker compose cp, без перезаписи прежнего файла.

Войти обычным аккаунтом с установленным постоянным паролем, обновить оболочку по кнопке, найти карточку «Проверка шифрования» и нажать «Проверить шифрование». Ожидаются сообщение об успешном восстановлении тестовых данных и четыре длительности. Повторный запуск может дать другое время. При уходе со страницы поздний результат игнорируется. Пароль/фраза пользователя не запрашиваются, тестовые данные и результаты не отправляются на сервер. Проверить Tab/Enter, видимость фокуса, прокрутку и отсутствие ошибок Console.

Параметры PBKDF2-SHA-256 (600000 итераций) предварительные до замера на iPhone. На телефоне по обычному HTTP эта проверка недоступна; не отключайте требования secure context. Desktop-замер не является результатом Safari/iPhone.

Точный контракт и ограничения: [CRYPTO_FORMAT.md](docs/CRYPTO_FORMAT.md). AES-256-GCM, контекстная аутентификация, упаковка ключа фразой и recovery, строгий JSON v1, предел записи 1 МиБ. Постоянный бюджет операций между устройствами ещё не реализован; MemoryEncryptionBudget применяется только в диагностике/тестах и не должен использоваться для реальных хранилищ.

Откат этапа 05 к 0.4.0 не требует отката схемы БД. Для пересборки через Git нужен сохранённый коммит этапа 04. На момент реализации агентом отдельного коммита 04 нет (последний c051f9c — этап 03); не считайте его эквивалентом отката 05 → 04. Уже существующий локальный образ tasks:0.4.0 совместим со схемой 3 и может быть выбран для того же app локальным Compose-override. Git-коммиты агент не создаёт без разрешения. Перед переключением кода сохраните текущие изменения и запишите коммит для возврата.

## История: обновление этапа 04 с этапа 03

Команды из корня проекта в PowerShell, по одной, с остановкой при ошибке. Текущая установка должна работать на 0.3.0 со схемой 2. Сначала сохранить старый образ и проверить новый, не запуская новый сервер на существующей БД:

```powershell
docker compose -f compose.yaml -f compose.ca.yaml build app
curl.exe -fsS http://localhost:3100/api/health
```

Запишите installationId и Git-коммит рабочей версии. Если дополнительный CA не нужен, уберите -f compose.ca.yaml. TLS-проверку не отключайте. Обычная сборка выполняет typecheck, build и все тесты; отдельные тестовые образы/контейнеры больше не создаются по решению пользователя. При сбое проверки новый образ не собирается, существующий сервис продолжает работать.

Создать снимок схемы 2 инструментом без миграций и выгрузить на хост:

```powershell
docker compose exec -T app node server/cli/backup.mjs /data/tasks.sqlite /data/before-stage04.sqlite
New-Item -ItemType Directory -Force -Path .\backups | Out-Null
$tasksContainer = docker compose ps -q app
docker cp "${tasksContainer}:/data/before-stage04.sqlite" .\backups\before-stage04.sqlite
```

При обновлении с 0.3.0 ожидается schemaVersion: 2. Если установка уже на 0.4.0, текущий снимок имеет схему 3 и не подходит для запуска 0.3.0. Если снимок существует, выберите новое имя; не перезаписывайте нужную копию на хосте. SQLite backup API учитывает WAL, затем выполняется integrity_check.

```powershell
docker compose -f compose.yaml -f compose.ca.yaml up -d --no-build --force-recreate app
docker compose ps
curl.exe -fsS http://localhost:3100/api/health
docker compose logs --tail 100 app
```

Ожидаются healthy, **0.4.0**, прежний installationId и увеличенный bootCount. Миграция 2 → 3 транзакционная. Существующий администратор не получает временный пароль; его пароль и действующие сессии сохраняются, в том числе после `docker compose restart app`.

## Пользователи и пароли

Кнопка «Пользователи» доступна администратору в диагностике: список логинов, ролей, дат создания, состояний пароля, создание обычного пользователя и выдача нового временного пароля. Дополнительные администраторы, смена логина/роли, блокировка и удаление отложены.

Пароль вводится вручную с повтором либо генерируется из 16 случайных символов. В результате можно скопировать пароль; после закрытия посмотреть его повторно нельзя. Передача пользователю выполняется администратором самостоятельно, без встроенной отправки сообщений. Буфер обмена может сохраняться в истории ОС; приложение не очищает его автоматически.

Временный пароль действует **48 часов от выдачи**, без продления при входе. До истечения срока нужно войти и сменить пароль. До смены доступны операции авторизации, собственная обязательная смена и выход. Управление аккаунтами заблокировано сервером, в том числе для восстанавливаемого admin. Публичный health и оболочка сохраняют своё назначение.

Обязательная смена отзывает все прежние сессии и создаёт новую текущую. Обычная смена доступна каждому: текущий пароль, новый, повтор и включённый по умолчанию флажок «Выйти на других устройствах». Снятый флажок сохраняет другие сессии. Новый пароль отличается от текущего. Сброс администратором всегда немедленно отзывает все сеансы.

Пароли не сохраняются открытым текстом в БД, логах и кешах. Поля находятся в памяти формы; браузер может отдельно предложить сохранение своим менеджером паролей. Перезагрузка очищает поля. Потеря ответа не вызывает автоматический повтор изменения: проверьте список/состояние, а после смены собственного пароля при потере сессии войдите с новым паролем. Повтор создания не создаёт дубль; повтор сброса со старой версией аккаунта отклоняется.

## Команды администратора

Нужен интерактивный терминал (без -T). Пароли не принимаются аргументом, ENV или pipe. Ручной ввод скрыт и требует повтора; Ctrl+C отменяет ввод. Сброс требует подтверждения вводом логина.

```powershell
docker compose exec app npm run user:create -- seconduser
docker compose exec app npm run user:create -- anotheruser --generate
docker compose exec app npm run user:reset -- seconduser --generate
docker compose exec app npm run admin:recover -- adminlogin --generate
```

Замените логины своими. user:reset работает только с обычным пользователем, admin:recover — только с существующим admin. Оба выдают временный пароль на 48 часов. CLI использует общий с UI сервис, не создаёт первого администратора, не мигрирует и не создаёт отсутствующую БД. CLI запускает оператор с доступом к серверу; HTTP требует сессию admin. Пароль выводится один раз после успеха и остаётся в истории вывода терминала, не в истории аргументов команд.

## Пользовательская проверка этапа 04

1. Войти прежним администратором без обязательной смены. При старой оболочке перезагрузить с сетью и нажать «Обновить приложение».
2. Создать тестового пользователя вручную: проверить неверный/занятый логин, правила пароля и несовпадающий повтор. Создать второго через генерацию; проверить копирование и закрытие результата.
3. В другом профиле браузера войти временным паролем. Обязательная смена показывает логин и срок; пропустить нельзя, перезагрузка возвращает к ней. Проверить неверный текущий, совпадающий с ним новый и несовпадающий повтор.
4. Сменить пароль и проверить вход новым: роль «Пользователь», нет управления пользователями. /api/auth/users возвращает запрет. Старый пароль больше не подходит.
5. В двух профилях войти одним тестовым аккаунтом. Обычная смена без флажка сохраняет второй сеанс; с флажком после следующей проверки/запроса потребуется вход. Сброс администратором прекращает все старые сессии.
6. Выдать новый временный пароль с подтверждением, повторно проверить обязательную смену. Ровно 48 часов покрыты тестом с управляемыми часами; менять системные часы сервера не нужно.
7. Проверить CLI создания/сброса на тестовом пользователе. Восстановление admin проверять на отдельной установке либо осознанно, имея доступ к серверу: оно завершит все его сеансы.
8. iPhone: прокрутка с клавиатурой, доступные кнопки, отсутствие горизонтального скролла, портрет/альбом и safe areas. Компьютер: Tab/Shift+Tab/Enter, видимый фокус, показ/скрытие паролей, назад/отмена.
9. Отключить сеть во время формы: понятная ошибка без ложного успеха. После восстановления проверить результат до повтора; перезагрузка очищает секретные поля. В Console нет JS/CSP-ошибок, в Local Storage/IndexedDB нет паролей/токенов, API не кешируется SW.
10. Перезапустить контейнер: аккаунты/пароли/сессии и installationId сохраняются. Через 16 минут вернуться во вкладку или выполнить действие: access продлевается через refresh.

Этап принят пользователем; перечень проверенных устройств не уточнялся. Браузерный инструмент агента вернул пустой список браузеров, поэтому собственную проверку Safari/PWA агент не заявляет. По решению пользователя агент пересобирает и обновляет существующий Compose-сервис; дополнительные тестовые контейнеры не создаёт.

## Первый администратор и новая установка

При пустой установке обе валидные переменные BOOTSTRAP_ADMIN_LOGIN и BOOTSTRAP_ADMIN_PASSWORD создают admin до открытия HTTP-порта. Обе отсутствуют/пусты — первый вход показывает предупреждение о роли и повтор пароля. Частичная/невалидная пара блокирует запуск без вывода значений. После настройки ENV игнорируется и не меняет пароль, даже после восстановления admin командой.

Если .env ещё нет, скопируйте .env.example и заполните локально. Он исключён из Git/Docker context. Пароль со знаком $ в .env заключайте в одинарные кавычки. После создания можно убрать bootstrap-данные и пересоздать контейнер. Без ENV первый посетитель ненастроенного приложения может стать admin — согласованный fallback. Для опубликованной установки задайте ENV заранее.

Новой установке backup старой БД не нужен: ENV, verify/build, compose up. Логин: 3–32 ASCII-символа, первая латинская буква, далее латиница/цифры/точка/дефис/подчёркивание, регистр не различается. Пароль: 6–128 Unicode-символов, цифра 0–9, верхний и нижний регистр, без обрезки пробелов/нормализации. Argon2id v19, 64 MiB, t=3, p=1, соль16, хеш32.

## Сессии и адрес приложения

Access — 15 минут; refresh — 30 дней от обновления, абсолютный предел 90 дней от входа. Для временного пароля дополнительно 48 часов от выдачи. Проверка на запуске, возврате во вкладку, восстановлении сети и перед операциями аккаунта. Токены opaque, HttpOnly-cookie; в SQLite только SHA-256-хеши.

Ротация refresh транзакционная. Повтор в течение 10 секунд возвращает ту же пару; поздний повтор старого токена отзывает семейство. Короткий повтор также доступен обладателю старого токена и CSRF-данных; бессрочного принятия нет. Потеря ответа вне окна может требовать входа. Web Locks координирует обновления вкладок при поддержке браузером.

Cookie SameSite=Strict, Path=/, без Domain; HTTPS добавляет Secure и __Host-. CSRF: exact Origin, JSON и заголовок с токеном из same-origin endpoint. Compose: APP_ORIGIN=http://localhost:3100, AUTH_COOKIE_MODE=localhost. Для HTTPS: точный APP_ORIGIN=https://tasks.DOMAIN, AUTH_COOKIE_MODE=secure. Nginx/HTTPS — отдельный этап; localhost компьютера не является адресом телефона.

Лимиты на процесс: 10 входов/15 минут на IP и логин, 120 изменяющих auth-запросов/минуту/IP, 20 операций аккаунтов с хешированием/минуту/IP, 10 попыток смены пароля/15 минут на аккаунт, максимум 2 параллельных Argon2 в HTTP. Счётчики сбрасываются при рестарте; CLI не объединяет лимиты с HTTP-процессом. За proxy IP пока является адресом соединения; trustProxy согласуется при HTTPS-развёртывании.

## Резервное копирование и откат этапа 04

Снимки содержат хеши паролей и сессий; ограничьте доступ. backups/ исключён из Git/Docker context. Текущий снимок схемы 3:

```powershell
docker compose exec app node server/cli/backup.mjs /data/tasks.sqlite /data/stage04-current.sqlite
```

Для отката код берётся из Git и пересобирается в том же Compose-сервисе. Сначала сохраните текущие изменения в Git, запишите текущий коммит для возврата и убедитесь, что рабочее дерево чистое. Нужен снимок схемы 2, сделанный до перехода на 0.4.0: Git не восстанавливает БД. Без него откат к 0.3.0 с сохранением данных не предусмотрен.

Подготовить текущий снимок и восстановить схему 2 в **новый каталог**, пока текущий контейнер ещё работает:

```powershell
docker compose exec app node server/cli/backup.mjs /data/tasks.sqlite /data/stage04-before-rollback.sqlite
docker compose exec app node server/cli/restore.mjs /data/before-stage04.sqlite /data/restored-before-stage04
@'
services:
  app:
    image: tasks:0.3.0
    environment:
      DATA_DIR: /data/restored-before-stage04
'@ | Set-Content -Encoding UTF8 backups/rollback-stage04.yaml
docker compose stop app
git switch --detach c051f9c
docker compose -f compose.yaml -f compose.ca.yaml build app
docker compose -f compose.yaml -f backups/rollback-stage04.yaml up -d --no-build --force-recreate app
curl.exe -fsS http://localhost:3100/api/health
```

Ожидаются 0.3.0 и прежний installationId из снимка. Схема 2 содержит состояние аккаунтов/паролей/сеансов на момент backup: поздние изменения отсутствуют, прежние пароли/сеансы могут снова действовать. Исходная схема 3 и её копия сохраняются. Старый образ не открывает схему 3. Restore откажет при существующем каталоге; выберите новое имя и измените DATA_DIR override согласованно. Не удаляйте WAL, данные сайта или Docker volume как штатный откат.

Не перезаписывайте ранее сохранённый backups/rollback-stage04.yaml; при необходимости выберите другое имя. После отката используйте сохранённый rollback-override. При возвращении к новому коммиту обычный compose снова выберет 0.4.0 и исходный /data; перед обновлением определите рабочую копию БД. Инструкции этапа 03 доступны в истории Git.

## Без Docker и файлы

Нужен Node 24.x: npm ci, npm run build, npm run check. Тестам нужен готовый dist. До открытия старой БД новой версией: `node server/cli/backup.mjs data/tasks.sqlite backups/before-stage04.sqlite` (каталог backups должен существовать).

Запуск: `node --env-file=.env server/index.mjs`, либо ENV и npm start. DATA_DIR по умолчанию ./data. CLI: npm run user:create -- LOGIN, npm run user:reset -- LOGIN, npm run admin:recover -- LOGIN; для генерации добавить --generate. Node не читает .env для npm-скриптов автоматически; нестандартный DATA_DIR задайте в окружении.

- src/components/Accounts.ts, Login.ts, src/auth.ts — формы и запросы.
- server/auth/accounts.mjs — общий сервис; sessions.mjs — токены/отзыв.
- server/routes/auth.mjs, server/cli/user.mjs — HTTP и команды.
- server/migrations.mjs, server/cli/backup.mjs, restore.mjs — схема и копии.
- tests/ — HTTP, права, сроки, конкурентность, миграции, backup/restore, скрытый ввод, модель SW.
- docs/PRODUCT_REQUIREMENTS.md, docs/UI_REFERENCE.md, docs/mockups/ — требования; TODO.md — ход этапов.

Runtime содержит production-зависимости, server, dist и package.json. Argon2 install-script разрешён только для закреплённой версии 0.45.1; TLS-проверка не отключается.
