{
  "name": "tasks",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "node scripts/build-client.mjs && node scripts/build-sw.mjs",
    "start": "node server/index.mjs",
    "check": "node --test tests/server.test.mjs tests/sw.test.mjs"
  },
  "dependencies": {
    "fastify": "5.12.3",
    "preact": "10.29.8"
  }
}
